{{ form_start(form, {method: 'POST', action: path('app_event_new', {'game_id': game.id})}) }}
    <div class="flex flex-col gap-3 sm:flex-row sm:items-end">
        <div class="sm:w-28">
            {{ form_row(form.timecode) }}
        </div>
        <div class="flex-1">
            {{ form_row(form.message) }}
        </div>
        <button class="inline-flex items-center justify-center rounded-md bg-slate-700 px-4 py-2 text-sm font-medium text-white hover:bg-slate-800" type="submit">
            Speichern
        </button>
    </div>
{{ form_end(form) }}
