{{ form_start(form, {method: 'POST', action: path('app_event_new', {'game_id':game.id})}) }}

<fieldset>
    <div class="row g-3">
        <div class="col-sm-2">
            {{ form_row(form.timecode) }}
        </div>
        <div class="col">
            {{ form_row(form.message) }}
        </div>
    </div>
    <div class="form-group">
        <button class="btn btn-primary pull-right" type="submit">
            Speichern
        </button>
    </div>
</fieldset>
{{ form_end(form) }}