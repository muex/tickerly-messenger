{% extends 'base.html.twig' %}

{% block title %}Ereignis hinzufügen{% endblock %}

{% block body %}
    <div class="card mx-auto max-w-lg rounded-3xl p-8">
        <h1 class="mb-2 text-2xl font-semibold tracking-tight text-ink-900">Ereignis hinzufügen</h1>
        <p class="mb-6 text-sm text-red-600">Bitte korrigiere die markierten Felder.</p>

        {{ include('game/_game_event_form.html.twig') }}

        <div class="mt-6 border-t border-black/10 pt-5">
            <a class="text-sm text-ink-500 transition hover:text-ink-900" href="{{ path('app_game_show', {'slug': game.slug}) }}">&larr; Zurück zum Spiel</a>
        </div>
    </div>
{% endblock %}
