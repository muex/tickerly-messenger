{{ form_start(form) }}
    <div class="space-y-4">
        {{ form_widget(form) }}
    </div>
    <button class="mt-5 inline-flex items-center justify-center rounded-md bg-slate-700 px-4 py-2 text-sm font-medium text-white hover:bg-slate-800">
        {{ button_label|default('Speichern') }}
    </button>
{{ form_end(form) }}
