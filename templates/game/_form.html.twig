{{ form_start(form) }}
    <div class="space-y-4">
        {{ form_widget(form) }}
    </div>
    <button class="btn-primary mt-6">
        {{ button_label|default('Speichern') }}
    </button>
{{ form_end(form) }}
