{% extends 'base.html.twig' %}

{% block title %}Spiel bearbeiten{% endblock %}

{% block body %}
    <div class="mx-auto max-w-lg rounded-lg bg-white p-6 shadow">
        <h1 class="mb-4 text-xl font-semibold text-slate-800">Spiel bearbeiten</h1>

        {{ include('game/_form.html.twig', {'button_label': 'Aktualisieren'}) }}

        <div class="mt-6 flex items-center justify-between border-t border-slate-200 pt-4">
            <a class="text-sm text-slate-600 hover:text-slate-900" href="{{ path('app_game_index') }}">&larr; Zurück zur Übersicht</a>
            {{ include('game/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
