/*
 * Application entrypoint loaded via AssetMapper (see importmap.php).
 * Styles are compiled by symfonycasts/tailwind-bundle and linked
 * directly in base.html.twig, so they are not imported here.
 */
